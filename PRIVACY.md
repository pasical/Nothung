# Nothung 隐私说明

生效日期：2026-08-11

Nothung 不收集、保存、出售或上传个人数据，不包含账户、广告、分析、遥测或第三方 SDK，也不保存链接历史。

## 本地处理

参数清理和正则替换均在设备上完成。自定义规则只保存在 App Group 中，供主 App、Action Extension 和 App Intent 共用。导入或导出只会在用户明确操作时发生。

Nothung 不在后台监听剪贴板。主 App 只通过系统“粘贴”控件读取用户明确选择的内容；“使用 Nothung 复制”只接收其他 App 通过系统分享面板提供的内容，并把处理结果写入剪贴板。

## 网络请求

只有展开重定向时才会访问网络。命中已启用重定向规则的 URL 可以自动展开；主 App 对其他 URL 会先请求确认；App Intent 始终离线处理。

展开时，完整 URL、IP 地址和普通连接信息会被目标网站及其网络服务商看到。Nothung 使用临时 URLSession，不发送已保存的 Cookie 或认证信息，只允许 HTTPS，最多跟随五次跳转，收到响应头后即取消响应正文，并尽力阻止本地、私有和保留地址。

使用系统分享功能时，清理结果只会交给用户选择的目标 App，并受 iOS 控制。

## 联系

本 GitHub 版本的隐私问题可以通过仓库 Issues 提交；请勿在 Issue 中附上私人链接或个人信息。修改代码或加入新的网络、日志、分析功能后，应同步更新本说明。
