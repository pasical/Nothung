# Nothung 隐私说明

生效日期：2026-08-16

Nothung 不收集、出售或上传个人数据，不包含账户、广告、分析、遥测或第三方 SDK。为了提供输入法自动捕捉与再次插入功能，它会在设备上保存有限的处理记录。

## 本地处理

参数清理和正则替换均在设备上完成。自定义规则只保存在 App Group 中，供主 App、Action Extension、Nothung 键盘和 App Intent 共用。导入或导出只会在用户明确操作时发生。

Nothung 不在后台运行或读取剪贴板。开启“允许完全访问”后，Nothung 键盘只在屏幕上可见期间检查系统剪贴板是否变化，并自动读取、清理和加入新文本；键盘收起或切换后即停止检查。主 App 只通过可见的系统“粘贴”控件读取用户明确选择的当前内容；“使用 Nothung 复制”只接收其他 App 通过系统分享面板提供的内容。

上述操作，以及主 App 中手动输入后明确点按“复制”，会把原始文本、清理结果和保存时间加入设备上的 Nothung 剪贴板集合，最多保留 20 条，相同内容自动去重。用户可以在 App 设置中清空全部记录，或在键盘中长按删除单条。记录不会上传；长按“显示原文”只在键盘内展示，不会插入原文。

## 网络请求

只有展开重定向时才会访问网络。命中已启用重定向规则的 URL 可以在主 App、分享扩展或键盘自动捕捉流程中自动展开；主 App 对其他 URL 会先请求确认；App Intent 始终离线处理。键盘中的既有记录无需联网即可插入。

展开时，完整 URL、IP 地址和普通连接信息会被目标网站及其网络服务商看到。Nothung 使用临时 URLSession，不发送已保存的 Cookie 或认证信息，只允许 HTTPS，最多跟随五次跳转，收到响应头后即取消响应正文，并尽力阻止本地、私有和保留地址。

使用系统分享功能时，清理结果只会交给用户选择的目标 App，并受 iOS 控制。

## 联系

本 GitHub 版本的隐私问题可以通过仓库 Issues 提交；请勿在 Issue 中附上私人链接或个人信息。修改代码或加入新的网络、日志、分析功能后，应同步更新本说明。
